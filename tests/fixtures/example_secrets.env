# Fixture used to validate Gitleaks allowlisting (issue #6).
# These are AWS's publicly documented EXAMPLE credentials — they are NOT real
# and grant no access. This file lives under tests/fixtures/, which is
# allowlisted in .gitleaks.toml, so it must NOT trigger the secret-scan gate.
AWS_ACCESS_KEY_ID=AKIAIOSFODNN7EXAMPLE
AWS_SECRET_ACCESS_KEY=wJalrXUtnFEMI/K7MDENG/bPxRfiCYEXAMPLEKEY
